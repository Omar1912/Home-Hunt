
# Azure DevOps Pipeline for GitHub Repo using Self-Hosted Agent
trigger:
  branches:
    include:
      - main  

pool:
  name: Default   #self-hosted agent pool

steps:
# Checkout GitHub repo
- checkout: self

# Example build step 
- script: echo "Hello from self-hosted agent!"
  displayName: 'Run a simple script'
